package com.jack.review.test.array;

import org.apache.commons.lang3.StringUtils;

import java.util.List;
import java.util.stream.Collectors;

/**
 * @Description:
 * @Auther: Jack You
 * @Date: 2021/05/13/10:30
 */
public class arrayToList {

    private static void test(){
        /*String productIds = rsp.getProductIds();
        if (StringUtils.isNotBlank(productIds)) {
            List<Long> productIdList = JSONArray.parseArray(productIds, Long.class);
            List<Product> products = productMapper.selectByProductIds(productIdList, LoginUserUtil.getCompanyCode());
            List<String> productFullNames = products.stream().map(Product::getProductFullName).collect(Collectors.toList());
            rsp.setProductNames(productFullNames.toString());
        }*/
    }
}
