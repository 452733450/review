（写完面试题直接就进来俩面试官，一个发问一个不吭声）
你用的java的版本，springboot的版本
    (我说java8  springboot这个我是真么注意过，，后来才知道是2.0)
Git回滚上版本用什么操作
(这个真不知道，就随便说了个，结果错了😭）
两个线程执行一个功能，怎么顺序执行（他举了个订单的例子，一个请求发来，线程a得先去查一次数据库某信息，如果信息核验正确，另一个线程再接着执行后面的操作）
(我说join()，他疑惑了嗯一声，就跳过了)
Mybatis操作数据库，datasource怎么配置的?
项目中表有多少字段，字段长度还记得么？
(20来个，我说表不是我设计的，长度忘记了，)
数据库表的主键用的是什么?
解释一下读写分离?
（我说写是轮询写在不同的表里，读的话mycat就发送语句到下面的表里去查，他再追问我比如有好几张表，要是在其中一张表里查不到怎么办，我就卡壳了）
Spring中你最熟悉的是什么?
Springboot常用的注解有哪些?
（我说controller，service那几个，还有component和configretion）
加了@configretion  的@bean还没法注入可能是什么情况，怎么解决？
（我说可能是没扫到包，加@componentScan试试）
怎么知道配置文件properties在哪里被读取（好像是这个意思，没录音，记不清了）（我回答说一般在resource里，他哦了一声）
Debug经常用么？（我说不常用他就走了😭后面另一个面试官就问了句薪资也走了）

读写分离是分库，一个库专门用来读，一个库专门用来写。
怎样解析请求并根据select/update/insert等不同业务类型分派到读库和写库，有两种方案。
1.用mycat一类中间件，配置好会自动分配。
2.用JAVA工程加AOP拦截业务层，分析当前业务类型，切换dataSource。