hashmap数据结构，
线程池创建里面的核心线程什么时候创建，队列什么时候创建，
聊聊分布式的CAP，
mysql优化，
redis热点key处理，
redis如何淘汰过期key的，
    可以使用KEYS 命令
    KEYS pattern

    例如， 列出所有的key
    redis> keys *

    列出匹配的key
    redis>keys apple*
        1) apple1
        2) apple2
redis展示所有key的命令，

spring ioc/di，
spring循环依赖怎么处理