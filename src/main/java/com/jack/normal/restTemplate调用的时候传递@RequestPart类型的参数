restTemplate调用的时候传递@RequestPart类型的参数
body.add("file", new ByteArrayResource(bytes) {
            @Override
            public String getFilename() {
                return file.getOriginalFilename();
            }
        });
没重写getFilename()方法的话，会报错
重写了就可以了