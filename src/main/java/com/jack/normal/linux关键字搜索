grep -i "销服短信任务，id" ./run.log


Linux less某个日志后 关键字搜索
ChatGPT
在 Linux 中使用 less 命令浏览日志文件，并进行关键字搜索是相当常见的操作。以下是使用 less 进行关键字搜索的步骤：

打开终端并导航到包含要查看的日志文件的目录。

输入以下命令，将日志文件以只读方式打开并使用 less 进行浏览：

Copy code
less 日志文件名
请将 "日志文件名" 替换为实际的日志文件名。

进入 less 的搜索模式，按下 / 键，然后输入要搜索的关键字，并按下回车键。

Copy code
/关键字
请将 "关键字" 替换为您要搜索的实际关键字。

less 将会高亮显示匹配的关键字，并显示第一个匹配结果。您可以按下 n 键以在日志中向下搜索下一个匹配项，按下 N 键以向上搜索上一个匹配项。

如果您想退出 less 浏览器，请按下 q 键。

使用上述步骤，您可以在使用 less 命令浏览日志文件时进行关键字搜索。这是一种快速而方便的方式来查找和定位特定关键字在日志中的出现位置。

zcat 日志文件.gz | less