Innodb和Myisam有什么区别
事务支持：
    MyISAM 不支持事务，这意味着在 MyISAM 表上不能使用事务操作，无法进行回滚和提交等操作。
    InnoDB 支持事务，可以使用事务操作，支持提交、回滚、ACID 属性等特性，提供了更好的数据完整性和一致性。
行级锁：
    MyISAM 表级锁：MyISAM 在执行写操作（INSERT、UPDATE、DELETE）时会对整个表进行锁定，可能会导致并发性能较差。
    InnoDB 行级锁：InnoDB 支持行级锁，可以在多个事务同时操作同一表时，只锁定需要操作的行，提高了并发性能和并发控制。
外键约束：
    MyISAM 不支持外键约束，不支持建立外键关系。
    InnoDB 支持外键约束，可以在表定义时指定外键关系，保证了数据的完整性和一致性。
崩溃恢复：
    MyISAM 不支持崩溃恢复，当数据库发生崩溃时可能会导致数据丢失或损坏。
    InnoDB 支持崩溃恢复和自动恢复机制，当数据库发生异常或崩溃时，可以自动恢复数据和索引。
表空间：
    MyISAM 表存储在三个文件中：.frm 文件存储表结构，.MYD 文件存储数据，.MYI 文件存储索引。
    InnoDB 表存储在表空间中，表空间由多个数据文件组成，数据和索引都存储在表空间中，可以更好地管理数据和索引