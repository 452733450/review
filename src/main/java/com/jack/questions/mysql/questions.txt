中通
数据量最多的是哪张表？有多少量
分库如何分的？开发中又遇到什么慢sql，如何优化？
in字段走索引吗？
    in后面的字段过多的话，就会分析消耗，如果走索引的时间还不如全表扫描的时间短，那就不会走索引
数据库的acid特性，特性是靠什么去实现的？
    **1.原子性和一致性：**在操作数据之前，首先将数据备份到一个地方（这个存储数据备份的地方称为 Undo Log）。然后进行数据的修改。如果出现了错误，系统利用 Undo Log 中的备份将数据恢复到事务开始之前的状态。
    **2.隔离性：**通过数据库锁的机制实现的
    **3.持久性：**和 Undo Log 相反，Redo Log 记录的是新数据的备份。在事务提交前，只要将 Redo Log 持久化即可，不需要将数据持久化。
    当系统崩溃时，虽然数据没有持久化，但是 Redo Log 已经持久化。系统可以根据 Redo Log 的内容，将所有数据恢复到最新的状态。
binlog、redolog、undolog是什么
    https://juejin.cn/post/6860252224930070536
    binlog 以二进制的形式保存数据库的写入性操作，sync_binlog可以设置几个事务后刷盘，0：系统自动判断，1：一个事务后刷盘，N:n个事务后
建索引一般在什么字段上建索引
表里有几个聚簇索引和非聚簇索引？  1个聚簇，多个非聚簇
    非聚簇索引（也叫二级索引，辅助索引）
    主键索引（聚簇索引）
建索引的时候，选择的索引的类型
    全文、普通、唯一、空间
b树和b+树的区别

众安
最左优先？举了个例子，问是否能用到索引？
like的时候什么情况下才能命中索引
    mysql在使用like查询的时候只有不以%开头的时候，才会使用到索引
    一般情况下不鼓励使用like操作，如果非使用不可，如何使用也是一个问题。like “%aaa%” 不会使用索引而like “aaa%”可以使用索引
数据库连接了一百多个，不用连接池，怎么处理

众安二面
数据库查询优化？
select  *  为什么就慢呢，你说查询单个字段块，为什么呢？
    https://mp.weixin.qq.com/s/uUj83OzuHm67R1yG-qajkw
mysql回表
    当mysql查询时，使用非聚簇索引（也叫二级索引，辅助索引） 查到相应的叶子节点获取主键值，然后通过 主键索引（聚簇索引） 再查到相应的数据行信息，找到主键后通过聚簇索引 找到相应数据行的过程叫做回表。

zk
如何进行的sql优化
mysql 索引失效的情况

极兔
mysql优化，

理想one
数据库聚簇索引和非聚簇索引
四大隔离级别
mysql默认隔离级别

蓝月亮
事务的隔离级别和mysql默认级别还有，