直接提交队列（Direct Submission Queue）:

任务被直接提交给线程池中的工作线程执行，而不经过队列。这种情况下，如果线程池中的线程都在执行任务，新的任务可能会被拒绝执行或者触发一些特定的策略，如创建新线程来执行任务。
无界队列（Unbounded Queue）:

无界队列可以无限制地存储任务，即使任务的提交速度超过了线程池中线程的执行速度。这样，如果有大量任务被提交，它们会在队列中等待执行。常见的无界队列类型包括 LinkedBlockingQueue。
有界队列（Bounded Queue）:

有界队列有一个最大容量限制，当队列达到最大容量时，新的任务可能会触发拒绝策略。有界队列可以帮助控制线程池中等待执行任务的数量，防止无限制的任务积压。常见的有界队列类型包括 ArrayBlockingQueue。
优先级队列（Priority Queue）:

优先级队列根据任务的优先级进行排序，每次从队列中选择具有最高优先级的任务进行执行。这种队列通常用于需要按照优先级执行任务的场景。Java 中的 PriorityBlockingQueue 就是一个常见的优先级队列实现。
延迟队列（Delay Queue）:

延迟队列用于存储具有激活时间的任务，只有当任务的激活时间到达时，才能被取出并执行。Java 中的 DelayQueue 是一个实现了延迟队列的类，常用于任务调度场景。