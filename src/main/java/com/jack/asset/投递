1.独立完成投资人个人中心，资产证明，产品风控，份额计算、业务分析，报表统计等功能模块的开发及一些原有模块的重构优化；
3.优化业务分析、统计报表模块，排查中接口响应慢、响应超时的问题，定位到问题为数据量大时用limit分页严重影响性能，因此采用记录上次返回的主键，在下次查询时使用主键过滤的方式，提高性能；
4.重构投资人个人中心模块的代码，使用设计模式来对投资人个人中心分多个不同币种的计算，包括投资总金额、成本、市值、露总收益等各种金额的计算,；
7.排查系统中出现的栈溢出的问题，经过jvm调优工具排查，定位问题，进行优化；
8.为解决处理文件和图片时的频繁GC问题，把垃圾回收器由默认的Parallel Scavenge GC和Parallel Old GC 替换为 ParNew GC和CMS GC及其他参数调优。

导入导出的封装
属性copy，注解反射，处理枚举映射
dao层查出来的数据，转换为easyExcel实体的过程
dubbo本地直连
导入
每行表头解析完触发，校验导入的模板是否正确
每行数据解析完触发 必填校验，小数位数校验，选项校验，日期小于等于当前日期校验

下载模板
    支持多个sheet
    定制模板样式

3.优化业务分析、统计报表模块，排查中接口响应慢、响应超时的问题，定位到问题为数据量大时用limit分页严重影响性能，因此采用记录上次返回的主键，在下次查询时使用主键过滤的方式，提高性能；