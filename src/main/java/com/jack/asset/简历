mysql innodb索引的类型
    主键索引
    唯一索引
    外键索引（Foreign Key Index）:
        用于创建表之间的关系。
        外键索引通常是对其他表的主键或唯一索引的引用。
    普通索引
    全文索引
        用于在文本数据上执行全文搜索。
        只能用于特定类型的列（例如，TEXT、VARCHAR）。

如果三台服务器都定义了相同的定时任务，也都加了分布式锁，有一个节点时间比其他两台服务器慢一分钟，这时会不会导致分布式锁失效
    会，幂等性问题，只需要在数据库中添加一个唯一标识，让后面的定时任务是否重复执行，xxl-job是利用mysql实现的乐观锁解决的这样的问题